const sqlite3 = require('sqlite3').verbose(); const db = new sqlite3.Database('C:/ProgramData/otzaria/books/seforim.db', sqlite3.OPEN_READONLY, err => { if (err) { console.error('open err', err); process.exit(1); } console.log('opened'); db.get('SELECT COUNT(*) AS n FROM line', (err, row) => { if (err) { console.error('query err', err); process.exit(1);} console.log('count', row.n); db.close(); }); });
